# Degeneration Axis

**Analyzing and mitigating LLM activation drift in agentic trajectories**

## Motivation

When LLMs operate as agents over long trajectories (50–100+ steps), do their internal activations drift systematically? If so, can we detect that drift via PCA and mitigate it with inference-time steering?

This project investigates activation drift on [SWE-bench](https://www.swebench.com/) trajectories generated by [mini-swe-agent](https://mini-swe-agent.com/), using Qwen-3-4B as the target model.

## Installation

```bash
git clone <repo-url>
cd degeneration_axis_dev
uv sync --extra dev
```

## Pipeline

The experiment is a 6-step pipeline. Each step reads from the previous step's output.

```
vLLM server → mini-swe-agent → .traj.json → parse → .jsonl → HF model replay → .pt activations → PCA → drift_axis.pt → steering
     0              1                           2                    3                  4                      5
```

### Step 0: Start vLLM server

Serves Qwen-3-4B locally with an OpenAI-compatible API.

```bash
./pipeline/0_vllm_server.sh
```

### Step 1: Collect trajectories

Runs mini-swe-agent on SWE-bench verified split. Each instance produces a `.traj.json` file with the full message history.

```bash
./pipeline/1_collect_trajectories.sh
```

Output: `data/drift/qwen3-4b/raw/*.traj.json`

### Step 2: Parse trajectories

Normalizes raw trajectory files into clean JSONL with `{instance_id, n_assistant_turns, conversation}`.

```bash
python pipeline/2_parse_trajectories.py
```

Output: `data/drift/qwen3-4b/parsed/trajectories.jsonl`

### Step 3: Extract activations (incremental replay)

For each trajectory with N assistant turns, performs N forward passes through the HuggingFace model. Pass k feeds the conversation up to turn k and mean-pools hidden states over the k-th assistant response at layers 26–35 (last 10 of 36).

```bash
python pipeline/3_drift_activations.py
```

Output: `data/drift/qwen3-4b/activations/*.pt` — tensors of shape `(N_turns, 10, 2560)`

### Step 4: Drift analysis

Pools activations across trajectories, runs PCA per layer, and tests whether PC1 correlates with turn index (Spearman correlation, linear regression). Produces drift curve plots, PCA scatters, and variance explained charts.

```bash
python pipeline/4_drift_analysis.py
```

Output: `data/drift/qwen3-4b/analysis/drift_axis.pt` and `data/drift/qwen3-4b/analysis/plots/*.html`

### Step 5: Drift steering (future)

Caps activations along the drift axis at early-turn thresholds (mean + 2σ from turns 0–4) using hook-based intervention during generation.

```bash
python pipeline/5_drift_steering.py
```

## Project structure

```
src/                          # Reusable library
  internals/
    model.py                  # ProbingModel — HF model loading, multi-GPU
    conversation.py           # ConversationEncoder — chat templates, Qwen span detection
    activations.py            # ActivationExtractor — hook-based hidden state extraction
    spans.py                  # SpanMapper — token span → activation mean pooling
  models.py                   # MODEL_CONFIGS (Qwen-3-4B, Gemma, Llama, Qwen-32B)
  pca.py                      # compute_pca, MeanScaler, plot_variance_explained
  axis.py                     # compute_axis, project, cosine_similarity_per_layer
  steering.py                 # ActivationSteering — addition, ablation, capping
  tests/
    test_axis.py

pipeline/                     # Experiment steps
  0_vllm_server.sh
  1_collect_trajectories.sh
  2_parse_trajectories.py
  3_drift_activations.py
  4_drift_analysis.py
  5_drift_steering.py
  configs/
    swebench_qwen4b.yaml      # mini-swe-agent config
    model_registry.json        # LiteLLM model registry

data/drift/qwen3-4b/          # Experiment data (gitignored)
  raw/                         # .traj.json files
  parsed/                      # trajectories.jsonl
  activations/                 # per-trajectory .pt files
  analysis/                    # drift_axis.pt, plots/
```

## Key concepts

**Incremental replay**: Rather than extracting activations from a single forward pass of the full trajectory, we replay the conversation incrementally — pass k includes only turns 0..k. This captures the model's actual hidden states as the context grows.

**Drift axis**: The PC1 direction from PCA on pooled activations. If activations drift systematically with turn index, PC1 will capture this and Spearman correlation will be significant.

**Activation capping**: A steering intervention that clamps the projection of activations onto the drift axis at a threshold. Projections below the threshold are untouched; those above are pulled back. This prevents late-turn activations from exceeding what's normal in early turns.

## Acknowledgments

Built on the [Assistant Axis](https://arxiv.org/abs/2601.10387) codebase. Uses [mini-swe-agent](https://mini-swe-agent.com/) for trajectory collection and [vLLM](https://vllm.ai/) for inference.

## License

MIT
